# =====================================================================
# TCC — Vision Transformers para Classificação de Alzheimer
# Ambiente: Python 3.11, Windows, CPU local (treino pesado vai para Colab)
# =====================================================================
# Como instalar (PowerShell, dentro de TCC/):
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
# =====================================================================

# --- Núcleo: PyTorch CPU ---
# Para CUDA, trocar pelo índice oficial: https://pytorch.org/get-started/locally/
# Versão fixa para reprodutibilidade entre as duas máquinas do aluno.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
torchvision==0.19.1+cpu

# --- Modelos pré-treinados (ViT, Swin, ResNet, DeiT, etc.) ---
timm==1.0.11

# --- Utilidades para imagem médica ---
monai==1.3.2

# --- Manipulação de imagens 2D (slices em JPEG) ---
Pillow==10.4.0
opencv-python-headless==4.10.0.84

# --- Análise de dados ---
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
scipy==1.14.1

# --- Visualização ---
matplotlib==3.9.2
seaborn==0.13.2

# --- Notebooks ---
jupyter==1.1.1
jupyterlab==4.2.5
ipywidgets==8.1.5
tqdm==4.66.5

# --- Métricas e relatórios extras ---
torchmetrics==1.4.3

# --- Reprodutibilidade e config ---
pyyaml==6.0.2

# --- Renderização de tabelas markdown (usado por pandas.DataFrame.to_markdown) ---
tabulate==0.9.0
